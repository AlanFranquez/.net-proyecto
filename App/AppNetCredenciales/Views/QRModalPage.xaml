<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="AppNetCredenciales.Views.QRModalPage"
             BackgroundColor="#80000000"
             Padding="30">

    <Frame BackgroundColor="White" CornerRadius="20" Padding="20" HorizontalOptions="Center" VerticalOptions="Center">
        <VerticalStackLayout Spacing="15" HorizontalOptions="Center">

            <Label Text="Tu QR para el evento" FontSize="18" FontAttributes="Bold" HorizontalOptions="Center" />

            <Image x:Name="QrImage" WidthRequest="300" HeightRequest="400" HorizontalOptions="Center" />

            <Button Text="Cerrar" Clicked="OnCloseClicked" />
        </VerticalStackLayout>
    </Frame>

</ContentPage>
