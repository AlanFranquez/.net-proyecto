POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=espectaculosdb
DB_PORT=5432
WEB_PORT=8080
AUTO_MIGRATE=true
AUTO_SEED=false

# --- Observabilidad / Logging ---
# URL de Seq (por defecto apunta al host desde el contenedor)
#SEQ_URL=http://host.docker.internal:5341
# Clave API de Seq si aplica
#SEQ_API_KEY=

# Endpoint OTLP (Collector corriendo en host)
#OTEL_EXPORTER_OTLP_ENDPOINT=http://host.docker.internal:4317

# CORS (en desarrollo)
#CORS_ORIGINS=http://localhost:5262,http://localhost:5173

# Tokens de validación (obligatorio en escenarios reales)
#VALIDATION_TOKENS__SECRET=local-dev-super-secret
#VALIDATION_TOKENS__DEFAULT_EXPIRY_MINUTES=120
