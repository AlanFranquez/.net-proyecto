# Configuración de entorno para k6 tests

# =============================================================================
# ENTORNO LOCAL (Docker)
# =============================================================================
# Nota: Si no se define BASE_URL, los scripts detectan automáticamente:
#   1. Si existe Terraform state con ALB → usa AWS
#   2. Si no existe → usa localhost:8080

# BASE_URL=http://localhost:8080

# =============================================================================
# ENTORNO AWS
# =============================================================================
# Los scripts run-all.ps1 y run-aws.ps1 detectan el ALB automáticamente desde Terraform.
# Solo necesitas definir BASE_URL manualmente si quieres sobreescribir la detección:

# BASE_URL=http://mi-alb-123456.us-east-1.elb.amazonaws.com

# =============================================================================
# OBSERVABILIDAD (Opcional - para enviar métricas a Prometheus remoto)
# =============================================================================
# K6_PROMETHEUS_RW_SERVER_URL=http://prometheus.miapp.com:9090/api/v1/write
# K6_PROMETHEUS_RW_TREND_AS_NATIVE_HISTOGRAM=true

# =============================================================================
# CONFIGURACIÓN ADICIONAL
# =============================================================================
# K6_OUT=json=results/output.json
# K6_SUMMARY_EXPORT=results/summary.json
